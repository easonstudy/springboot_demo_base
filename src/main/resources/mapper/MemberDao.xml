<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gmh.dao.MemberDao">

	<select id="getMemberById" resultType="Member">
		SELECT * FROM T_MEMBER WHERE ID=#{id}
	</select>

</mapper>